#include<iostream>
#include<vector>

using namespace std;

int main(){
    int n, m;
    vector<int> ar;
    cin >> n >> m;
    ar.resize(n+2);
    ar[0] = 0;
    for(int i = 1; i <= n; i++) cin >> ar[i];
    ar[n+1] = m;
    bool state = ()
}
