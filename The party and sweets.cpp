#include<bits/stdc++.h>

using namespace std;

int main(){
    int n, m;
    cin >> n >> m;
    vector<int> b(n), g(m);
    for(int& i: b) cin >> i;
    for(int& i: g) cin >> i;
    sort(b.begin(), b.end());
    sort(g.begin(), g.end());
    if(b[n - 1] > )
}